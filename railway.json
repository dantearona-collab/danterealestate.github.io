{
  "$schema": "https://railway.app/schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "uvicorn cms_con_ia:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/health"
  }
}
