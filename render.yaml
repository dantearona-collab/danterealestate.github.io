services:
  - type: web
    name: dante-propiedades
    env: python
    rootDir: backend
    pythonVersion: 3.12.3
    plan: free
    autoDeploy: true
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main-ai:app --host 0.0.0.0 --port $PORT
    envVars: []
